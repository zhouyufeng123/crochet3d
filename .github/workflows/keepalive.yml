name: keep-alive
on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: 保活请求
        run: curl -s -o /dev/null -m 60 https://crochet3d.onrender.com/api/health
